{
  "name": "cli",
  "type": "module",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/cli.js --minify --format=cjs --platform=node",
    "cli": "./index.js",
    "lint": "eslint . --ext .js --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "esbuild": "^0.14.38"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-modern": "^0.6.7"
  }
}
